package com.jarvis.app.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated, understated palette: black/charcoal + muted bronze/amber +
// silver. Not a simple light/dark inversion - each theme is hand-tuned to
// keep the same identity (metal + soft light, not an "AI" look).

// ---- Light theme: warm off-white, charcoal text, deepened bronze for contrast ----
val LightBackground = Color(0xFFF3EFE7)
val LightSurface = Color(0xFFFAF7F1)
val LightSurfaceVariant = Color(0xFFEFE6D6)
val LightOnBackground = Color(0xFF2B2926)
val LightOnSurfaceVariant = Color(0xFF7A756D)
val LightOutline = Color(0xFFE6E0D4)
val LightPrimary = Color(0xFF9C7A46) // muted bronze, deepened for light-bg contrast
val LightOnPrimary = Color(0xFFFFF8EF)
val LightSilver = Color(0xFF87858A)

// ---- Dark theme: near-black / soft charcoal, warm off-white text ----
val DarkBackground = Color(0xFF121113)
val DarkSurface = Color(0xFF1C1B1E)
val DarkSurfaceVariant = Color(0xFF242226)
val DarkOnBackground = Color(0xFFEDE9E2)
val DarkOnSurfaceVariant = Color(0xFFA6A29B)
val DarkOutline = Color(0xFF2A282C)
val DarkPrimary = Color(0xFFB08D57) // muted bronze/amber metal, not bright orange
val DarkOnPrimary = Color(0xFF241C10)
val DarkSilver = Color(0xFF9C9BA0)
